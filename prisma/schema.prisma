generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  directUrl  = env("DIRECT_URL")
}

model AdminUser {
  id           String         @id @default(cuid())
  email        String         @unique
  passwordHash String
  name         String
  createdAt    DateTime       @default(now())
  sessions     AdminSession[]
  articles     Article[]      @relation("ArticleAuthor")
}

model AdminSession {
  id        String    @id @default(cuid())
  token     String    @unique
  admin     AdminUser @relation(fields: [adminId], references: [id])
  adminId   String
  expiresAt DateTime
  createdAt DateTime  @default(now())
}

model SiteUser {
  id           String        @id @default(cuid())
  email        String        @unique
  passwordHash String
  name         String
  role         SiteUserRole  @default(READER)
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  sessions     SiteSession[]
  articles     Article[]     @relation("SiteUserArticles")
}

model SiteSession {
  id        String   @id @default(cuid())
  token     String   @unique
  user      SiteUser @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId    String
  expiresAt DateTime
  createdAt DateTime @default(now())
}

model Article {
  id            String     @id @default(cuid())
  title         String
  slug          String     @unique
  excerpt       String
  content       String
  category      String
  status        String     @default("Draft")
  author        String
  featuredImage String?
  publishedAt   DateTime?
  views         Int        @default(0)
  likesCount    Int        @default(0)
  commentsCount Int        @default(0)
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt
  authorRef     AdminUser? @relation("ArticleAuthor", fields: [authorId], references: [id])
  authorId      String?
  siteAuthor    SiteUser?  @relation("SiteUserArticles", fields: [siteAuthorId], references: [id])
  siteAuthorId  String?
}

enum SiteUserRole {
  READER
  AUTHOR
}
